{
  "name": "zyne-property-buy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "prepare:property-images": "node scripts/prepare-property-images.mjs",
    "validate": "node scripts/validate-listing.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run validate && npm test && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "sharp": "^0.34.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@fontsource/roboto": "^5.2.10"
  }
}
